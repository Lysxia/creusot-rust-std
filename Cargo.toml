[package]
name = "verif-slice"
version = "0.1.0"
edition = "2024"

[dependencies]
creusot-std = "0.11.0-dev"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(creusot)'] }

[features]
optimize_for_size = []
